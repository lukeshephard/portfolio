import { Cutive_Mono, Geist, Geist_Mono, Shadows_Into_Light } from "next/font/google";

export const geist = Geist({
  subsets: ["latin"],
});

export const geistMono = Geist_Mono({
    subsets: ["latin"]
})

export const shadowsIntoLight = Shadows_Into_Light({
    weight: "400",
    subsets: ["latin"]
})
